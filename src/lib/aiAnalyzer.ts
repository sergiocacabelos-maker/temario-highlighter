import type { AnalyzerResult } from './types';

// Placeholder: la integración real con OpenAI debe hacerse desde backend seguro.
export async function analyzeWithAI(_text: string): Promise<AnalyzerResult | null> {
  return null;
}
